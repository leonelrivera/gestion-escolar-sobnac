# ==========================================
# PLANTILLA DE CONFIGURACIONES (EJEMPLO)
# ==========================================
# Este archivo SÍ se sube a Git. Sirve como guía de qué variables necesita el sistema.
# Copia este archivo como ".env" en producción y completa los valores.

# --- BASE DE DATOS (POSTGRES) ---
# (Inventa un usuario y una contraseña fuerte. La DB se creará usándolos).
DB_USER=escribe-el-usuario-aqui
DB_PASSWORD=escribe-la-contraseña-aqui
DB_NAME=escribe-el-nombre-de-la-db-aqui

# --- BACKEND (NESTJS) ---
# (Cualquier texto largo y aleatorio. Es vital para la seguridad de los logins).
JWT_SECRET=escribe-tu-secreto-jwt-aqui

# --- URLS ---
# (Descomenta y cambia si el backend restringe el origen de las peticiones Frontend)
# FRONTEND_URL=https://tudominio.com
